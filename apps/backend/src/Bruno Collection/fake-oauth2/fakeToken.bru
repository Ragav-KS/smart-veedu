meta {
  name: fakeToken
  type: http
  seq: 4
}

post {
  url: {{apiBaseUrl}}/fake-oauth2/fakeToken?grant_type=authorization_code
  body: none
  auth: inherit
}

params:query {
  grant_type: authorization_code
}

body:form-urlencoded {
  grant_type: authorization_code
}

script:pre-request {
  req.setMaxRedirects(0);
}
